        run: |
          KNOWLEDGE=$(find knowledge_base -maxdepth 1 -name 'task_*.py' -print0 | xargs -0 cat 2>/dev/null | tail -c 5000)
          PROMPT="You are the ARCHITECT AI. 
          OBJECTIVE: $OBJECTIVE. 
          CURRENT_CORE_FILES: $KNOWLEDGE. 
          
          CRITICAL INSTRUCTION:
          1. Every module MUST have a 'run(*args, **kwargs)' function.
          2. The 'run' function should be the ONLY entry point used for execution.
          3. If you need to perform different actions (like read/write), use an 'action' parameter inside the 'run' function.
          4. Ensure all Assertions in your test cases match the logic of your 'run' function.
          
          Task: Write the next evolution of the AI core. Respond ONLY with raw Python code."